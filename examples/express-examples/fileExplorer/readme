il server riceve richieste HTTP e risponde con le info sul file corrispondente al path, se esistente.

`client.js` prende come argomento il numero di richieste, manda richieste con path random e calcola il numero di richieste per secondo.
