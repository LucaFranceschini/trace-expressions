The client sends a HTTP request, then it receives the response and prints it.

The correct server writes the header just once.

The buggy server writes the header twice for the same response.

This is not a very interesting example.
